diff -r closure/node_modules/pi-subagents/src/agents/agents.ts w7/pkg/src/agents/agents.ts
--- closure/node_modules/pi-subagents/src/agents/agents.ts	2026-09-12 15:57:53.953711951 +0800
+++ w7/pkg/src/agents/agents.ts	2026-09-13 00:14:32.543594898 +0800
@@ -602,6 +602,7 @@
 }
 
 function collectPackageSubagentPaths(cwd: string, options: { includeUser: boolean; includeProject: boolean } = { includeUser: true, includeProject: true }): PackageSubagentPaths {
+	if (codifySubagentDiscoveryIsolated()) return { agents: [], chains: [], watchPaths: [], settingsErrors: {} };
 	const agentDir = getAgentDir();
 	const projectRoot = findConfiguredProjectRoot(cwd) ?? cwd;
 	const packageRoots: Array<{ root: string; scope: PackageScope }> = [
@@ -892,7 +893,18 @@
 	return path.join(getAgentDir(), "settings.json");
 }
 
+// Codify vendor patch: the cloned repository is untrusted. Project agent
+// definitions, project settings (provider/model/thinking overrides,
+// disableBuiltins, agentScanDirs, defaultExtensions) and package-provided
+// subagents all live inside that workspace, so under the ceiling they are
+// hidden and only the definitions Codify writes into the task-local Pi home
+// remain visible (open-harness-v2-subagent-adaptation.md §6.4).
+function codifySubagentDiscoveryIsolated(): boolean {
+	return process.env.CODIFY_PI_SUBAGENT_ISOLATED === "1";
+}
+
 function getProjectAgentSettingsPath(cwd: string): string | null {
+	if (codifySubagentDiscoveryIsolated()) return null;
 	const projectRoot = findConfiguredProjectRoot(cwd);
 	return projectRoot ? path.join(getProjectConfigDir(projectRoot), "settings.json") : null;
 }
@@ -2219,6 +2231,7 @@
 }
 
 function resolveNearestProjectAgentDirs(cwd: string): { readDirs: string[]; candidateDirs: string[]; preferredDir: string | null } {
+	if (codifySubagentDiscoveryIsolated()) return { readDirs: [], candidateDirs: [], preferredDir: null };
 	const projectRoot = findConfiguredProjectRoot(cwd);
 	if (!projectRoot) return { readDirs: [], candidateDirs: [], preferredDir: null };
 
@@ -2233,6 +2246,7 @@
 }
 
 function resolveNearestProjectChainDirs(cwd: string): { readDirs: string[]; preferredDir: string | null } {
+	if (codifySubagentDiscoveryIsolated()) return { readDirs: [], preferredDir: null };
 	const projectRoot = findConfiguredProjectRoot(cwd);
 	if (!projectRoot) return { readDirs: [], preferredDir: null };
 
diff -r closure/node_modules/pi-subagents/src/runs/background/top-level-async.ts w7/pkg/src/runs/background/top-level-async.ts
--- closure/node_modules/pi-subagents/src/runs/background/top-level-async.ts	2026-09-12 15:57:54.231719466 +0800
+++ w7/pkg/src/runs/background/top-level-async.ts	2026-09-13 00:14:22.327007987 +0800
@@ -12,3 +12,18 @@
 	if (params.foregroundOnly || !(depth === 0 && forceTopLevelAsync)) return params;
 	return { ...params, async: true, clarify: false };
 }
+
+// Codify vendor patch (open-harness-v2-subagent-adaptation.md §6.4).
+//
+// The parent Harness only sees a child's identity, usage and final output in
+// the launch's structured result, and a detached launch returns an empty child
+// inventory. The ceiling therefore pins depth-0 launches to the foreground
+// path; the model can no longer opt into background work.
+export function applyForceTopLevelForegroundOverride<T extends AsyncOverrideParams>(
+	params: T,
+	depth: number,
+	forceTopLevelForeground: boolean,
+): T {
+	if (!(depth === 0 && forceTopLevelForeground)) return params;
+	return { ...params, async: false };
+}
diff -r closure/node_modules/pi-subagents/src/runs/foreground/subagent-executor.ts w7/pkg/src/runs/foreground/subagent-executor.ts
--- closure/node_modules/pi-subagents/src/runs/foreground/subagent-executor.ts	2026-09-12 15:57:54.215899924 +0800
+++ w7/pkg/src/runs/foreground/subagent-executor.ts	2026-09-13 00:14:22.346861455 +0800
@@ -106,7 +106,7 @@
 import { getExternalJobProvider } from "../../api/external-job-provider.ts";
 import { externalJobFollowUpRequestDigest, externalJobFollowUpRequestId, externalJobFollowUpRunId, externalJobPromptDigest, externalJobStableJson } from "../shared/external-job-runner.ts";
 import { externalCliReceiptMetadata, normalizeExternalCliRunnerStatus } from "../shared/external-cli-contract.ts";
-import { applyForceTopLevelAsyncOverride } from "../background/top-level-async.ts";
+import { applyForceTopLevelAsyncOverride, applyForceTopLevelForegroundOverride } from "../background/top-level-async.ts";
 import { handleMissionAction, MISSION_ACTIONS } from "../../missions/actions.ts";
 import { attachMissionToLaunchResult, prepareMissionLaunch, writeMissionAsyncBinding, type MissionLaunchBinding } from "../../missions/lifecycle.ts";
 import { MissionNotFoundError, updateMission } from "../../missions/store.ts";
@@ -4901,7 +4901,16 @@
 		deps.state.lastForegroundControlId ??= null;
 		const normalizedGate = normalizeGateParams(params);
 		if (!normalizedGate.ok) return buildRequestedModeError(params, normalizedGate.error);
-		let requestParams = normalizedGate.params;
+		// Codify vendor patch: the workflow branch below treats a missing `async`
+		// as async (`requestParams.async !== false`) and detaches the run to a
+		// background runner, whose child inventory, usage and output never reach
+		// the parent stream. Every other override site runs after that decision,
+		// so the depth-0 foreground pin is applied here, at the entry of execute().
+		let requestParams = applyForceTopLevelForegroundOverride(
+			normalizedGate.params,
+			checkSubagentDepth(deps.config.maxSubagentDepth, deps.childRuntime).depth,
+			deps.config.forceTopLevelForeground === true,
+		);
 		const capacityOverrideError = validateWorkflowCapacityOverrides(requestParams);
 		if (capacityOverrideError) return buildRequestedModeError(requestParams, capacityOverrideError);
 		let workflowPreflight: import("../../shared/types.ts").WorkflowPreflight | undefined;
@@ -6677,10 +6686,14 @@
 		if (normalized.error) return normalized.error;
 		const normalizedParams = normalized.params!;
 
-		let effectiveParams = applyForceTopLevelAsyncOverride(
-			normalizedParams,
+		let effectiveParams = applyForceTopLevelForegroundOverride(
+			applyForceTopLevelAsyncOverride(
+				normalizedParams,
+				depth,
+				deps.config.forceTopLevelAsync === true,
+			),
 			depth,
-			deps.config.forceTopLevelAsync === true,
+			deps.config.forceTopLevelForeground === true,
 		);
 		const runToolBudget = resolveToolBudget(
 			effectiveParams.toolBudget,
@@ -7227,7 +7240,11 @@
 		const requestParams = normalizedAction ? { ...params, action: normalizedAction } : params;
 		if (normalizedAction) return execute(id, requestParams, signal, onUpdate, ctx).then(withAggregatedToolUsage);
 		const { depth } = checkSubagentDepth(deps.config.maxSubagentDepth, deps.childRuntime);
-		const dispatchParams = applyForceTopLevelAsyncOverride(requestParams, depth, deps.config.forceTopLevelAsync === true);
+		const dispatchParams = applyForceTopLevelForegroundOverride(
+			applyForceTopLevelAsyncOverride(requestParams, depth, deps.config.forceTopLevelAsync === true),
+			depth,
+			deps.config.forceTopLevelForeground === true,
+		);
 		const runsForeground = dispatchParams.clarify === true || (dispatchParams.async ?? deps.asyncByDefault) !== true;
 		if (!runsForeground) return execute(id, requestParams, signal, onUpdate, ctx).then(withAggregatedToolUsage);
 		if (deps.state.subagentInProgress === true) return duplicateSubagentCallResult(requestParams);
@@ -7262,6 +7279,22 @@
 		if (loaded.error) {
 			return Promise.resolve({ content: [{ type: "text", text: loaded.error }], isError: true, details: { mode: publicParams.action ? "management" : "workflow", results: [] } });
 		}
+		// Codify ceiling (open-harness-v2-subagent-adaptation.md §6.4): a
+		// background top-level launch returns no child inventory on the parent
+		// stream, so the ceiling forbids it outright instead of relying on
+		// downstream paths to honour the foreground override. Rejecting the
+		// call tells the model exactly how to retry, and no child can ever run
+		// detached from a Codify Task.
+		if (loaded.params) {
+			const { depth } = checkSubagentDepth(deps.config.maxSubagentDepth, deps.childRuntime);
+			if (depth === 0 && deps.config.forceTopLevelForeground === true && (loaded.params as { async?: boolean }).async === true) {
+				return Promise.resolve({
+					content: [{ type: "text", text: "Background delegation is disabled for this workspace. Call the subagent tool again without `async` (or with `async: false`) so the run executes in the foreground." }],
+					isError: true,
+					details: { mode: "single", results: [] },
+				});
+			}
+		}
 		publicExecutions.add(loaded.params!);
 		return executeWithSingleDispatchGuard(id, loaded.params!, signal, onUpdate, ctx);
 	};
diff -r closure/node_modules/pi-subagents/src/shared/types.ts w7/pkg/src/shared/types.ts
--- closure/node_modules/pi-subagents/src/shared/types.ts	2026-09-12 15:57:54.234476008 +0800
+++ w7/pkg/src/shared/types.ts	2026-09-13 00:14:22.296495097 +0800
@@ -2585,6 +2585,8 @@
 	/** Experimental observer: mirror each native subagent's progress into a new Orca tab. */
 	orcaProgressTabs?: OrcaProgressTabsConfig;
 	forceTopLevelAsync?: boolean;
+	/** Codify ceiling: pin depth-0 launches to the foreground path. */
+	forceTopLevelForeground?: boolean;
 	waitTool?: WaitToolConfig;
 	defaultSessionDir?: string;
 	singleRunOutputBaseDir?: string;
