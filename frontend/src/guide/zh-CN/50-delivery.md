---
title: 交付
section: User Guide
tier: core
---
## 分支与提交

![一条需求一条工作分支；是否创建 MR 由需求设置决定，终点都是关闭需求](assets/diagrams/zh-CN/delivery-path.svg)

> [!route] **交付原则**：同一需求的所有回合共用一条工作分支；是否创建 Merge Request 只决定交付分岔，不会改变任务的执行与提交过程。

工作分支名为 `codify/issue-{id}`，其中 `id` 是 Codify 内部的 Issue id，不是 GitLab 的 issue IID。分支名只写在需求上，之后注入该需求的每个容器，因此同一需求的所有任务共用同一个名字。

系统不会提前创建工作分支。第一次需要它时，由容器内的 Worker 在本地创建：本地已有同名分支就直接检出，只有远端有该分支就基于远端分支创建本地分支，两边都没有则从基线分支新建。分支只在第一次成功推送时才出现在远端。基线分支的取值顺序是需求指定的基线分支、目标分支、项目默认分支。

任务详情页的「任务概览」里，「分支配置」一行列出这条分支链路：

| 字段 | 说明 |
|------|------|
| 基线分支 | 创建工作分支时使用的起始分支 |
| 工作分支 | 任务使用的工作分支，需求内所有回合共用 |
| 目标分支 | 合并请求的目标分支 |
| 直接推送 | 任务背后没有需求、拿不到工作分支时，显示在工作分支的位置上 |

没有需求的手动任务，「来源」一行显示「手动创建」。

起始分支与仓库准备方式都配置在需求上：大型仓库可以在需求的高级设置里改「仓库克隆方式」（完整克隆 / 浅克隆）、「历史深度」和「延迟下载历史文件内容」。如果起始分支在远端不存在，系统会回退到远端默认分支并留下警告。

一个需求对应一条工作分支和一个工作区。所有任务都提交到这条分支，后续任务直接复用上一个任务留下的检出结果，不会重新克隆仓库，因此从已有的提交继续。工作区在任务之间保留，包括上一个任务还没提交的改动。分析模式的任务会丢弃改动而不提交，任务仍然算成功完成。

你和 Codify 写的是同一条分支。如果你把自己的提交推送到这条工作分支，下一次执行会快进到这些提交之上并在其上继续，不会重写或 rebase 历史。如果远端分支已经前进，而持久工作区里还有未提交的改动，仓库准备会直接拒绝，不会覆盖本地改动；两边历史已经分叉、或远端分支被回退时同样拒绝。遇到这种情况，请先在 GitLab 上把分支对齐，或在一个新需求上继续，新需求会使用全新的工作区。

「提交记录」区块展示本次执行产生的提交：

- 「本次提交 ({count})」：本次执行新增的提交，含提交 SHA 与摘要。
- 「前序任务提交 ({count})」：从工作区里继承的、尚未确认推送的前序提交。

提交信息由模型根据改动文件与 diff 统计生成。实施模式若未开启「要求代码变更」，没有改动也能以「无变更，无需推送」收尾；开启后则会直接失败。

推送结果记为下列状态之一：

| 推送状态 | 含义 |
|----------|------|
| 已推送 | 分支已成功推送到远端，交付确认 |
| 远端已包含 | 远端已有这些提交，无需再次推送 |
| 无变更，无需推送 | 本次执行没有产生任何改动 |
| 未尝试推送 | 执行没有走到推送阶段 |
| 推送失败（交付未确认） | 推送被拒绝或未获确认，交付不算完成 |

推送比对的是哪一份远端状态、推送被拒绝时会带上哪些错误码，见《交付内部》。

## 合并请求

交付以 Merge Request 为主。任务概览里的「合并请求」一行给出这个需求当前的 MR 状态：

- MR 已存在时，这一行链到 GitLab，显示 `!{iid}`。
- MR 还没创建、但需求设了目标分支时，这一行显示「将创建 MR」和目标分支。
- 需求没有目标分支时，这一行显示「不创建 MR」。

创建需求时，「MR 设置」里的「创建合并请求」开关决定需求有没有目标分支：开启时显示「将创建 MR」，关闭时显示「不创建 MR」，并说明「禁用时仅推送分支，不创建 MR」。需求详情页的「交付概览」用「代码交付」汇总这一状态，显示「已生成 MR」或「等待交付」。

MR 的生命周期由 Codify 自动管理：执行开始时会先创建一条 Draft MR，打上 `Codify` 标签；Codify 拿到管理员令牌、可以模拟发起人时，以发起人的 GitLab 身份创建，否则使用机器人账号。任务成功交付后去掉 Draft 标记，并用需求标题和需求下全部回合的交付摘要重写标题与描述。工作分支是 MR 的源分支，需求变更目标分支不会创建新的 MR，而是复用该分支上已打开的那一条。

关闭需求时，界面会先询问如何处理工作分支，可选「关闭并保留分支」或「关闭并删除分支」；删除操作不可撤销。如果开启了「分支清理」，MR 合并触发的 webhook 自动关闭需求时会删除 AI 工作分支；未开启则保留。

## 变更与用量统计

每个任务会记录两类量化指标，它们同时进入仪表盘与统计分析的趋势图。

**代码变更**来自执行过程中采集的 diff 事实，包含新增、删除、总行数，以及新增 / 修改 / 删除的文件数。任务列表与详情页把它显示为 `+新增 -删除`，无文件净变化时显示「无净文件变化」。如果这次执行没有采集到统计，会显示「统计未采集」，而不是显示零。

**Token 用量**来自 Harness 上报的用量事件，分为「输入」与「输出」两项，显示在任务详情页的「运行统计」区块里。事件流里子代理会单独标出各自的 Token 消耗，上下文压缩以「上下文已压缩」标出；「运行统计」区块汇总这次执行的模型、时长与 Token，并在发生过压缩时显示「上下文压缩」次数（形如 `{count}次`）。

需求详情页把同一需求下所有回合累加为「变更」（附新增行数与删除行数的 `+` `-` 明细）、「总耗时」和合并的「Token」，方便判断一条需求整体的代价；需求列表里再把 Token 拆成「输入」与「输出」两行。

没有 Token 数据的任务在「运行统计」的 Token 一项显示 `-`。手动修正任务状态只改状态本身，不会重算 Token 与代码变更数字，但按状态统计的完成与失败口径会跟着变。

两项统计都在任务收尾时写入：任务仍在执行时，任务页不显示「提交记录」与「运行统计」；任务列表也支持按「变更」与「耗时」排序，方便找出代价最大的执行。

## 运行归档

运行归档收录哪些内容、大小上限与保留策略，以及「下载运行归档」入口的位置，见《交付内部》。
